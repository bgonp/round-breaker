{% extends 'base.html.twig' %}

{% block title %}Nueva contraseña{% endblock %}

{% block content %}
    <h1>Nueva contraseña</h1>
    {{ form_start(requestForm) }}
    {{ form_label(requestForm.email, 'Introduzca su nueva contraseña.') }}
    {{ form_widget(requestForm.email, {'attr': {'class': 'mb-2 form-control'}}) }}
    <button class="mt-2 btn btn-primary btn-dark sport-text form-control">Cambiar contraseña</button>
    {{ form_end(requestForm) }}
{% endblock %}